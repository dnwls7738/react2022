# 프로젝트 에이전트 지침

## 기본 원칙

- 모든 설명, 코드 주석, 문서, 커밋 메시지는 한국어로 작성한다.
- 사용자가 명시적으로 중단하거나 범위를 제한하지 않는 한, 필요한 명령 실행과 검증을 매번 묻지 않고 자동으로 진행한다.
- 기존 사용자 변경 사항을 삭제하거나 되돌리지 않는다. `git reset --hard`, `git checkout --`, 강제 덮어쓰기 같은 명령은 사용하지 않는다.
- 요청 범위와 무관한 리팩터링은 하지 않는다.
- 파일을 수정한 뒤에는 가능한 가장 좁은 범위의 테스트나 빌드를 먼저 실행하고, 실패하면 같은 범위에서 원인을 수정한 뒤 다시 검증한다.

## 실행 및 검증

- 의존성이 없으면 `npm install`을 실행한다.
- 개발 서버는 필요할 때 `npm start`로 실행하며, 장시간 실행되는 서버는 검증 후 종료한다.
- 기본 검증 순서는 다음과 같다.
  1. `npm test -- --watchAll=false`
  2. `npm run build`
- 테스트나 빌드 결과와 실패 원인, 해결 여부를 작업 후 `README.md`에 한국어로 기록한다.
- 외부 API가 필요한 기능은 API 키와 네트워크 의존성을 확인하고, 사용할 수 없는 경우 그 사실과 검증 범위를 README에 명시한다.

## 변경 및 Git

- 수정 전 현재 작업 트리와 사용자 변경 사항을 확인한다.
- 검증이 끝난 변경만 커밋한다.
- 커밋 메시지는 한국어로 작성하고 변경 목적을 짧게 설명한다.
- 사용자가 명시적으로 요청한 경우에만 원격 저장소에 push한다. 이 지침의 기본 동작은 로컬 Git 커밋까지다.

## 프로젝트 구조

- 앱 진입점은 `src/index.js`, 라우팅과 전역 구성은 `src/App.js`에 있다.
- 라우트 화면은 `src/components/pages/`, 재사용 콘텐츠는 `src/components/includes/`, 공통 레이아웃은 `src/components/layout/`, 기본 UI는 `src/components/basics/`에 둔다.
- 전역 스타일 진입점은 `src/style.scss`이며 도메인별 SCSS partial은 `src/assets/styles/`에 둔다.
- 컴포넌트 파일의 기존 `.jsx` 확장자와 주변 코드의 스타일을 유지한다.
- CSS 클래스는 기존 BEM 유사 규칙을 따르고, 페이지와 콘텐츠의 책임을 섞지 않는다.

## 데이터 및 환경 변수

- 영화와 YouTube 기능은 외부 API에 의존한다.
- 필요한 환경 변수는 `REACT_APP_MOVIE_API`와 `REACT_APP_API`이며 비밀 값은 저장소에 커밋하지 않는다.
- 포트폴리오와 Reference 데이터도 외부 JSON 요청에 의존할 수 있으므로 네트워크 실패를 고려한다.
- API 호출, 검색, 라우팅, GSAP 애니메이션을 변경하면 관련 동작을 우선 검증한다.

## 테스트 작성

- 기존 테스트는 `src/App.test.js`와 CRA Jest 환경을 기준으로 확장한다.
- 외부 API, 타이머, GSAP, 직접 DOM 접근은 테스트에서 mock 또는 격리한다.
- 테스트가 부족한 영역을 발견하면 무관한 대규모 테스트 개편 대신 변경한 동작에 필요한 최소 테스트를 추가한다.