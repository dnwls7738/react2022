# 프로젝트 에이전트 지침

## 작업 원칙

- 모든 설명, 코드 주석, 문서, 테스트 설명, 커밋 메시지는 한국어로 작성한다.
- 사용자가 중단하거나 범위를 제한하지 않는 한 필요한 명령 실행과 검증을 묻지 않고 자동으로 진행한다.
- 기존 사용자 변경 사항을 삭제하거나 되돌리지 않는다. `git reset --hard`, `git checkout --`, 강제 덮어쓰기를 사용하지 않는다.
- 요청 범위와 무관한 리팩터링은 하지 않는다.
- 파일을 수정한 뒤에는 변경 범위에 가장 가까운 테스트나 빌드를 먼저 실행한다. 실패하면 같은 범위에서 원인을 수정하고 다시 검증한다.

## 실행 및 검증

- 의존성이 없으면 `npm install`을 자동 실행한다.
- 개발 서버가 필요한 경우 `npm run dev` 또는 `npm start`를 사용하고, 검증이 끝난 장기 실행 서버는 종료한다.
- 기본 검증 순서는 `npm test` 후 `npm run build`다.
- 외부 API, 네트워크, API 키가 필요한 기능은 해당 조건을 확인하고, 검증할 수 없는 범위와 실패 원인을 README에 한국어로 기록한다.
- 테스트와 빌드 결과, 변경 요약, 남은 주의점은 항상 `README.md`에 한국어로 갱신한다.

## 변경 및 Git

- 작업 시작 전에 `git status`로 브랜치와 사용자 변경 사항을 확인한다.
- 검증에 성공한 변경만 커밋한다.
- README 기록을 포함한 관련 변경을 `git add`한 뒤 `git commit`으로 자동 커밋하고, 커밋 메시지는 변경 목적을 요약한 한국어로 작성한다.
- 원격 저장소가 설정되어 있고 인증이 가능한 경우 커밋 후 원격 반영까지 자동으로 시도한다. 인증 실패나 원격 오류는 README와 작업 결과에 기록하고, 기존 변경을 되돌리지 않는다.
- 커밋 또는 원격 반영이 실패해도 소스 변경은 보존하고 실패 원인을 보고한다.

## 프로젝트 구조

- Vite 진입 HTML은 `index.html`, React 진입점은 `src/index.jsx`, 라우팅은 `src/App.jsx`다.
- 페이지 컴포넌트는 `src/components/pages/`, 재사용 콘텐츠는 `src/components/includes/`, 공통 레이아웃은 `src/components/layout/`, 기본 UI는 `src/components/basics/`에 둔다.
- 전역 스타일 진입점은 `src/style.scss`이고 도메인별 SCSS partial은 `src/assets/styles/`에 둔다.
- 컴포넌트는 기존 함수형 및 클래스형 패턴과 파일 확장자 `.jsx`를 존중한다.
- CSS 클래스는 `header__menu`, `movie__search` 같은 기존 BEM 유사 명명 규칙을 따른다.
- 페이지, 레이아웃, 콘텐츠의 책임을 섞지 않고 기존 컴포넌트 경계를 유지한다.

## 데이터 및 환경 변수

- 영화와 YouTube 기능은 외부 API에 의존하므로 API 키, 네트워크, CORS, 사용량 제한을 확인한다.
- 비밀 값은 저장소에 커밋하지 않는다. `.env*` 파일은 `.gitignore` 규칙을 따른다.
- 기존 코드의 `REACT_APP_*` 환경 변수 사용과 Vite의 `import.meta.env.VITE_*` 규칙이 일치하는지 확인한 뒤 변경한다.
- 포트폴리오와 Reference 데이터도 외부 JSON 요청에 의존할 수 있으므로 네트워크 실패를 고려한다.
- API 호출, 검색, 라우팅, GSAP 애니메이션을 변경하면 해당 동작을 우선 검증한다.

## 테스트 작성

- 테스트는 Vitest와 jsdom, Testing Library 설정을 따른다. 관련 설정은 `vite.config.mjs`와 `src/setupTests.js`를 참고한다.
- 외부 API, 타이머, GSAP, 직접 DOM 접근은 테스트에서 mock하거나 격리한다.
- 테스트가 부족한 영역을 발견하면 무관한 대규모 개편 대신 변경한 동작에 필요한 최소 테스트를 추가한다.
- 저장소의 `build/`와 `dist/`는 산출물일 수 있으므로 소스 코드의 최신 상태를 확인할 때 기준으로 삼지 않는다.